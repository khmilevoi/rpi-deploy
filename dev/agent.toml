# dev-режим (Windows/локально): TCP вместо unix-сокета, данные в ./.dev-data
data_dir = ".dev-data"
tcp = "127.0.0.1:7700"

# [cloudflared]                 # v0.2: auto-ingress; omit to disable
# config = "/var/lib/pi/cloudflared/config.yml"
# tunnel = "home"
